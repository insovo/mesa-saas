# 通用
.DS_Store
*.log
.vscode/
.idea/

# Node / 构建产物(阶段① 引入 — 仅作用于 server/ 和 web/)
node_modules/
dist/
.cache/

# 环境变量 — 严禁入库
.env
.env.local
.env.*.local
!.env.example

# Prisma 本地
server/prisma/*.db

# 容器卷数据(本机持久化,不入库)
.docker-data/

# TLS 证书 / 私钥 — 永远不入库,仅 VPS 本地持有
web/certs/
*.pem
*.key

# Playwright 临时输出
.playwright-mcp/
*.png

# 交付文档源 md(只保留 docx)— 可选,保留 md 也行
# delivery-docs/src/
